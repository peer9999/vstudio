#include <Windows.h>
int WINAPI WinMain (
	HINSTANCE hInstance,
	HINSTANCE hPrevInstance,
	LPSTR	  lnCmdLine,
	int		  nCmdShow)
{
	MessageBox(NULL, L"дружок-пирожок ты ошибся дверью boy next door", L"Петушок)))", MB_OK);
	return NULL;
}